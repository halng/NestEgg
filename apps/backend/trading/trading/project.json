{
  "name": "trading-trading",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./gradlew clean build -x test",
        "cwd": "apps/backend/trading/trading"
      },
      "outputs": ["{workspaceRoot}/apps/backend/trading/trading/build"]
    }
  }
}