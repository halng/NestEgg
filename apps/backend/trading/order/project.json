{
  "name": "trading-order",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./gradlew clean build -x test",
        "cwd": "apps/backend/trading/order"
      },
      "outputs": ["{workspaceRoot}/apps/backend/trading/order/build"]
    }
  }
}