{
  "name": "identity-user",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./gradlew clean build -x test",
        "cwd": "apps/backend/identity/user"
      },
      "outputs": ["{workspaceRoot}/apps/backend/identity/user/build"]
    }
  }
}