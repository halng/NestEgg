{
  "name": "gateway",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./gradlew clean build",
        "cwd": "apps/backend/gateway"
      },
      "outputs": ["{workspaceRoot}/apps/backend/gateway/build"]
    }
  }
}